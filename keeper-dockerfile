FROM golang:1.25.3

RUN apt-get install && apt-get install -y git

WORKDIR /app

COPY go.mod go.sum ./

RUN go mod download

COPY packages ./packages/
COPY cmd/keeperserver/main.go .

RUN go build -o keeperserver main.go

CMD [ "./keeperserver" ]
